/**
 * @file dummy_test.cpp
 * @brief Minimal smoke-test used to validate CI/CD workflow builds.
 *
 * This executable verifies that the project compiles successfully with the
 * configured toolchain and returns exit-code 0, satisfying CTest and
 * Valgrind pipeline steps.
 */

int main() {
    return 0;
}
